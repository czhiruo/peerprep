import { CollabRequest } from "../models/collabRequest";
import { sendMessage } from '../kafka/producer';

export async function getQuestionForRoom(collabRequest: CollabRequest): Promise<void> {
    // Replace with actual question retrieval
    
}